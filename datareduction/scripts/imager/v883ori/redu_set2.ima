! Reduce V883-Ori setting 2 data

sic parallel

@ ../lib/redu init

let redu%basename v883ori
let redu%setting 2
@ set_redu_params

! Increase clipping value for line detection
let redu%uv_preview_clip 5

! Spectral window 25 has a few channels with different weights. Ignore
! these differences so that we can use a single beam over the entire
! band.
let beam_gap 10

@ ../lib/redu run
@ ../lib/redu clean
