! Reduce HOPS-373 setup 8 data

sic parallel

@ ../lib/redu init

let redu%basename hops373
let redu%setting 8
@ set_redu_params

! Spectral window 31 has a few channels with different weights. Ignore
! these differences so that we can use a single beam over the entire
! band.
let beam_gap 8

@ ../lib/redu run
@ ../lib/redu clean
